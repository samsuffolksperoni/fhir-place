---
"@fhir-place/react-fhir": minor
---

Add `useTypedSearch` hook: wraps `searchBuilder` in a TanStack Query hook with cache-invalidation parity with `useSearch` via shared `fhirQueryKeys.search` keys.
